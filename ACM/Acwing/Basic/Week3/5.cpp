#include<bits/stdc++.h>

using namespace std;

typedef long long ll;
int cnt;
int t;
int k,n;
string s;
bool solve()
{
    //比他小的字符串
    ll cnt;
    int len = (int)s.length();
    while(s[0] != 'a')
    {
        for(int i = len;i>=)
    }
}
int main()
{
    //条件1.1  2 3
    //条件2：回文串
    //先字典序 后回文串// 回文串好判断
    
    scanf("%d",&t);
    for(int k = 1;k<=t;k++)
    {
        
        scanf("%d %d",&n,&k);
        cin>>s;
        solve();
    }



    return 0;
}